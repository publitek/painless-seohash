apply plugin: 'elasticsearch.esplugin'

esplugin {
  name 'painless-seohash'
  version '6.4.0'
  description 'Whitelisted SEO hasing function'
  classname 'SeoHashPlugin'
  extendedPlugins = ['lang-painless']
}

dependencies {
  compileOnly project(':modules:lang-painless')
}

test.enabled = false
